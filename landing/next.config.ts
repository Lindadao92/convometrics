import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Existing HTML pages live in /public and are served as static files.
  // Next.js App Router handles /onboarding, /settings, and all /api routes.
};

export default nextConfig;
